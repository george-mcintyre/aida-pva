!!edu.stanford.slac.aida.lib.model.AidaProvider
# Channels definition file for aida-pva: SLC data provider
# This file defines the available channels for the SLC aida service
#
#  Supply the 'Type' parameter to choose the return type, e.g. Type=FLOAT
#  Supply the 'Value' parameter to call setValue() set a float value e.g. Value=78.32
#
id: 8
name: SLC
description: Aida SLC Data Provider
getterConfig:
  type: ANY
  fields:
    - name: value
      label: "Device data"
      description: "SLC DB Device Data for named device"
setterConfig:
  type: VOID
channels:
  - channel: "DB::*:????:*:*"
